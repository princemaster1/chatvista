# Backend — ChatVista

Node.js + Express backend with Socket.IO.

Important:
- Uses `bcryptjs` for password hashing (Termux compatible).
- JWT for auth.

Scripts:
- `npm run migrate` — runs SQL migrations (requires psql & env DATABASE_URL)
- `npm start` — starts the server
